# Copy this file to secrets.auto.tfvars (do not commit that file).

# Option 1 (recommended): load local API env file and push to Secrets Manager.
api_env_file_path = "../../../api/.env"

# Option 2: inline env content directly (leave empty if using file path).
api_env_content = ""

# Worker secret source
worker_env_file_path = "../../../whatsapp/whatcommerce/.env"
worker_env_content   = ""
