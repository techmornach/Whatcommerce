# Prod Terraform environment

Copy the `dev` environment structure into this directory and adjust:
- CIDR ranges
- tags/environment name
- backend state key
- production scaling, security, and deletion protection settings
